import { useStageStore } from "../stores/StageConfig.js";
import { nanoid } from "nanoid";

// 保存机器
function saveMachine(data) {}

// 更新机器
function updateMachine(data) {}

// 旋转机器
function rotateStorageMachine(data) {}

// 删除机器
function dropMachine(data) {}

export { saveMachine, updateMachine, rotateStorageMachine, dropMachine };
